import 'package:flutter/material.dart';

final kDarkThemeData = ThemeData.from(
  colorScheme: const ColorScheme.dark().copyWith(
    primary: const Color(0xFFC576F6),
    secondary: const Color(0xFFC576F6),
  ),
);

final kLightThemeData = ThemeData.from(
  colorScheme: const ColorScheme.light().copyWith(
    primary: const Color(0xFF880ED4),
    secondary: const Color(0xFF880ED4),
  ),
);
